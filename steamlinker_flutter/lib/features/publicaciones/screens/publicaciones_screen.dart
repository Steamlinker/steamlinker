import 'package:flutter/material.dart';

class PublicacionesScreen extends StatelessWidget {
  const PublicacionesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Publicaciones'));
  }
}